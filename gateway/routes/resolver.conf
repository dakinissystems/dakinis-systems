# Railway private DNS (default for production gateway on Railway).
# Docker Compose dev overrides this file — see docker/nginx/resolver.local.conf + compose.dev.yml.
resolver [fd12::10] ipv6=on valid=5s;
