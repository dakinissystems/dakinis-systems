/** Secciones del shell oficial — mismo esqueleto en todas las apps. */
export const DES_SHELL_SECTIONS = [
  "sidebar",
  "header",
  "content",
  "footer",
  "notifications",
  "commandPalette",
  "search",
];

/**
 * Dashboard canónico — todos los productos parten de aquí.
 * Sidebar → Topbar → Widgets → Cards → Timeline → Quick Actions
 */
export const DES_DASHBOARD_SECTIONS = [
  "sidebar",
  "topbar",
  "widgets",
  "cards",
  "timeline",
  "quickActions",
];
