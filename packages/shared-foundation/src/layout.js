/** Layout size tokens (JS mirror of CSS --dakinis-*-width/height). */
export const DAKINIS_LAYOUT = {
  headerHeight: 56,
  sidebarWidthRem: 16,
  sidebarCollapsedRem: 4.5,
  cardPadding: 16,
  dialogWidthRem: 28,
  contentMaxPx: 1100,
};

/** Icon sizes (px) — Lucide / shared-icons. */
export const DAKINIS_ICON_SIZES = {
  xs: 16,
  sm: 20,
  md: 24,
  lg: 32,
  xl: 48,
};
