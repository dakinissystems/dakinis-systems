{
  "name": "dakinis-billing",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Platform billing service — plans, Stripe, invoices, usage",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "health": "node -e \"fetch('http://127.0.0.1:'+(process.env.PORT||4080)+'/health').then(r=>r.json()).then(console.log)\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@dakinis/shared-ai": "file:./packages/shared-ai",
    "bullmq": "^5.41.0",
    "pg": "^8.13.1",
    "redis": "^5.5.6",
    "stripe": "^22.3.2"
  }
}
