# Core — conocimiento para RAG

## Copilot / Business Advisor

- Intents: clientes inactivos, stock bajo, caducidad lotes, catálogo, proveedores.
- Endpoint interno Core: `/api/internal/intelligence/*` consumido por `platform/ai`.
- UI: `BusinessNavHeroAskAi` en dashboard comercial.

## Planes

- Starter, Growth, Pro — ver landing `/#precios` y Stripe Payment Links.

## Inventario

- Alertas stock mínimo, lotes FIFO/FEFO, caducidad vía `expiring-lots`.

Ver también: `docs/legal/`, contratos OpenAPI en `docs/contracts/`.
