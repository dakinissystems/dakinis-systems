{
  "service": "auth",
  "gatewayPrefix": "/auth/",
  "upstreamContainer": "auth:4000",
  "description": "GET /auth/verify: 200 + cabeceras de identidad (p. ej. X-User-Id, X-Tenant-Id, X-User-Role) y X-Auth-Status ok si JWT válido; 401 si no. El gateway usa solo el código HTTP del subrequest (sin if por cabecera); la caché Nginx puede acortar roundtrips.",
  "publicRoutePrefixes": ["/auth/"],
  "examplePaths": [
    "/auth/login",
    "/auth/register",
    "/auth/me",
    "/auth/verify",
    "/auth/health"
  ]
}
