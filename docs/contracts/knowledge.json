{
  "service": "dakinis-knowledge",
  "status": "scaffold",
  "version": "0.3.0",
  "description": "Platform Knowledge — documents, policies, FAQ, RAG sources. AI consumes Knowledge.",
  "baseUrl": "https://api.dakinissystems.com/knowledge",
  "auth": "Bearer INTERNAL_API_KEY (service-to-service)",
  "routes": {
    "GET /knowledge/health": "Health",
    "GET /knowledge/v1/sources": "List source types",
    "POST /knowledge/v1/query": "Query corpus (proxies Search when configured)",
    "POST /knowledge/v1/ingest": "Enqueue document ingest",
    "GET /knowledge/v1/documents": "List documents (slug and/or tenantId)",
    "GET /knowledge/v1/documents/:id": "Get document by id"
  },
  "schema": "knowledge",
  "repository": "https://github.com/dakinissystems/dakinis-knowledge",
  "scaffold": "../../knowledge/",
  "consumers": ["ai", "hub", "core", "internal-api"],
  "notes": [
    "Separate from AI LLM service and Search index",
    "Embeddings stored via Search / pgvector roadmap"
  ]
}
