name: Postgres backup

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    if: github.repository == 'dakinissystems/dakinis-systems'
    steps:
      - uses: actions/checkout@v4

      - name: Backup note
        run: |
          echo "Scheduled backup runs against your Postgres host."
          echo "Configure BACKUP_DATABASE_URL secret and uncomment pg_dump step when ready."
          echo "Local dev: .\\scripts\\backup-postgres.ps1"

      # Descomentar cuando BACKUP_DATABASE_URL esté en GitHub Secrets:
      # - name: pg_dump to artifact
      #   env:
      #     DATABASE_URL: ${{ secrets.BACKUP_DATABASE_URL }}
      #   run: |
      #     pg_dump "$DATABASE_URL" | gzip > backup.sql.gz
      # - uses: actions/upload-artifact@v4
      #   with:
      #     name: postgres-backup-${{ github.run_id }}
      #     path: backup.sql.gz
      #     retention-days: 14
