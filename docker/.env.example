# Copia este archivo a `.env` en esta misma carpeta (`docker/`).
# Solo se usa para interpolación de Docker Compose (p. ej. ${ENV} en compose.full.yml).
#
#   Copy-Item .env.example .env
#
# Valores típicos: dev | prod (resuelve `env_file: .env.${ENV:-dev}` → `.env.dev` o `.env.prod`).

ENV=dev
