# Nginx — nota histórica

El **gateway activo** del stack local vive en **[`gateway/`](../../gateway/)** (Nginx + `routes/` + `middleware/`). Ese árbol es el que monta `docker/compose.full.yml`.

Esta carpeta se mantiene solo como referencia o apuntes; no es la configuración que usa el compose principal.
